export type PagesT = {
  link: string;
  name: string;
};

export type FooterT = {
  tittle: string;
};

export type ContactsT = {
  icon?: string;
  text: string;
};

export type SocialT = {
  icon: string;
  text: string;
};
